# This file makes the 'service' directory a Python sub-package
# within the 'design_mapper' service.
#
# It will typically contain modules related to the core business logic
# of the design mapper, such as the mapping functions that translate
# brand references and style cues into concrete theme tokens and
# Tailwind CSS classes.
