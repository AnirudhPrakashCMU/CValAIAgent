# This file makes the 'api' directory a Python sub-package
# within the 'orchestrator' service.
#
# It will typically contain modules related to API endpoints,
# such as WebSocket handlers and REST API route definitions.
